version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: circleci/node:16.15.1
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: |
            npm install --save
      - run:
          name: Run Unit Tests
          command: |
            npm run test
  build:
    docker:
      - image: circleci/node:16.15.1
    steps:
      - checkout
      - run: npm run start
workflows:
  my-custom-workflow:
    jobs:
      - run_test
      - build