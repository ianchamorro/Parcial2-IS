version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node
        auth:
          username: ianchamorro1
          password: dockerpass
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: |
            npm install --save
      - run:
          name: Run Unit Tests
          command: |
            npm run test
      - run: 
          name: Execute app.js
          command: |
            npm run start
workflows:
  build_test:
    jobs:
      - build