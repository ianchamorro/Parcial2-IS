version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/node
        #auth:
          # ensure you have first added these secrets
          # visit app.circleci.com/settings/project/github/ianchamorro/parcial2-is/environment-variables
          #username: $DOCKER_HUB_USER
          #password: $DOCKER_HUB_PASSWORD
jobs:
  run-app:
    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          npm run dev
workflows:
  my-custom-workflow:
    jobs:
      - run-app