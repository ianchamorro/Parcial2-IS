version: 2.1
jobs:
  run_tests:
    docker:
      - image: circleci/node:16.15.1
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: |
            npm install --save
      - run:
          name: Run Unit Tests
          command: |
            npm run test
  build:
    docker:
      - image: circleci/node:16.15.1
    steps:
      - checkout
      - run: npm run start
workflows:
  build_test:
    jobs:
      - run_test
      - build